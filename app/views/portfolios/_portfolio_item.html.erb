<h5> <%= link_to portfolio_item.title, portfolio_show_path(portfolio_item) %></h5>

<em><%= portfolio_item.subtitle %></em>

<p> &nbsp; &nbsp;<%= portfolio_item.body %></p>

<h5> <%= image_tag portfolio_item.thumb_image unless portfolio_item.thumb_image.nil? %></h5>

<p> <%= link_to "Edit Item", edit_portfolio_path(portfolio_item.id)  if logged_in? ( :site_admin )  %> </p>
<p> <%= link_to "Delete", portfolio_item, method: :delete,
										 data: {confirm: "Are you sure want to do"}  if logged_in? ( :site_admin ) %> </p>
<hr>